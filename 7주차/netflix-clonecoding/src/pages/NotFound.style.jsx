import styled from "styled-components";

export const NotFound = styled.div`
    margin: 30px;

    button {
        border: 0;
        outline: 0;
        padding: 0;
        background-color: transparent;
        color: red;
        font-size: 1.2rem;
        cursor: pointer;
    }
`;